include:
  - project: fe/inf/devops
    ref: master
    file: /browser/singleRepo/.ci.base-with-docker.yml

variables:
  IMAGE_NAME: changxi
  PROJECT_DIR_PATH: pc
  PACKAGE_MANAGE: pnpm
  LINT: 'true'
  TEST: 'true'
